// libc interop: strlen returns size_t (==long in tinyC).
extern long strlen(char *s);

int main() {
    _tinyc_print(strlen(""));
    _tinyc_print(strlen("hello"));
    _tinyc_print(strlen("hello world"));
    return 0;
}
