// Basic `double` (TY_F64) support: literal, arithmetic, comparison,
// casts to/from int and float, and printing.
int main() {
    double a;
    double b;
    a = 1.5;
    b = 2.25;
    double s;
    s = a + b;
    _tinyc_print(s);            // 3.75

    double p;
    p = a * b;
    _tinyc_print(p);            // 3.375

    double n;
    n = -a;
    _tinyc_print(n);            // -1.5

    int i;
    i = (int)b;
    _tinyc_print(i);            // 2

    double d;
    d = (double)7;
    _tinyc_print(d);            // 7

    float f;
    f = 3.5f;
    double w;
    w = (double)f;
    _tinyc_print(w);            // 3.5

    if (a < b) _tinyc_print(1); else _tinyc_print(0);  // 1
    return 0;
}
