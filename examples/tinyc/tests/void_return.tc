// `void` return type with a bare `return;` early exit.
void greet(int n) {
    if (n < 0) {
        _tinyc_print(-1);
        return;
    }
    _tinyc_print(n);
}

int main() {
    greet(7);     // 7
    greet(-3);    // -1
    greet(42);    // 42
    return 0;
}
