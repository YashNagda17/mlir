struct R { int a; int b; };

int main() {
    struct R one;
    one.a = 100; one.b = 200;
    struct R *p = &one;
    _tinyc_print((int64_t)p[0].a);
    _tinyc_print((int64_t)p[0].b);
    return 0;
}
