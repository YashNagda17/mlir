// multi_global: a.tc forward-declares the global `counter` (extern),
// b.tc provides the definition. The parser merges tentative+def into
// the initialized definition.

extern int counter;

int main() {
    _tinyc_print(counter);    // 10
    counter = counter + 5;
    _tinyc_print(counter);    // 15
    return 0;
}
